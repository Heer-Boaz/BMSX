#include "scratchbuffer.h"

namespace bmsx {

// Header-only template implementation.
// This translation unit is kept so the utility exists as a concrete C++ source
// file and can host explicit instantiations later if needed.

} // namespace bmsx
