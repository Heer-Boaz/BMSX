export default function schedule({ logger }) {
	setTimeout(() => {
		// const results = globalThis.$.world.getWorldObject('p3.text.results');
		// logger(`[assert] p3.text.results.full_text_lines: ${JSON.stringify(results.full_text_lines)}`);
		// if (results.full_text_lines.length <= 1) {
		// 	throw new Error('[assert] Expected combat results to include reward lines.');
		// }
		// No-op for now
	}, 1000);
}
