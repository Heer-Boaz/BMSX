import { test } from 'node:test';

// Temporarily disabled: see tests/lua/disabled/vm_runtime.integration.test.ts
// Re-enable once Reviver/serialization mocks are restored for the console runtime suite.
test.skip('console runtime integration suite temporarily disabled', () => {});
